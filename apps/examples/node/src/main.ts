import { examplesJsBuildable } from '@garage/examples/js-buildable';
import { examplesNodeBuildable } from '@garage/examples/node-buildable';
console.log('Hello World!');
console.log(examplesJsBuildable());
console.log(examplesNodeBuildable());
