# =============================================================================
# AWS RDS Configuration Template (for migration testing script)
# =============================================================================
#
# This file is used ONLY by scripts/test-migration-with-prod-data.sh
# It is NOT loaded by Nx during normal app execution.
#
# Nx .env Loading Order (highest to lowest precedence):
#   1. .env.local  - Local overrides (gitignored, store secrets here)
#   2. .env        - Defaults (committed)
#
# To use the migration testing script:
#   1. Copy the AWS RDS section from .env.local (uncomment it first)
#   2. Paste actual values here
#   3. Run: ./scripts/test-migration-with-prod-data.sh dump
#
# =============================================================================

# Database Configuration - AWS RDS
DB_HOST=soccer-stats-dev-postgres.xxxxxxxx.us-west-2.rds.amazonaws.com
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-password-here
DB_NAME=soccer_stats
DB_SYNCHRONIZE=false
DB_SSL=true
