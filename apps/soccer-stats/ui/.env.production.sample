# Production Environment Variables for Soccer Stats UI
# Configure these in Vercel dashboard under Project Settings > Environment Variables

# API URL - Your Railway deployment URL
VITE_API_URL=https://your-api.up.railway.app

# Note: CLERK_PUBLISHABLE_KEY is no longer needed here!
# The Clerk publishable key is now fetched at runtime from the API endpoint:
# GET /api/config/public
# This follows 12-factor app principles and allows the same build
# to be deployed to different environments.
