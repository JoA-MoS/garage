# gitlab-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gitlab-client` to build the library.

## Running unit tests

Run `nx test gitlab-client` to execute the unit tests via [Jest](https://jestjs.io).
