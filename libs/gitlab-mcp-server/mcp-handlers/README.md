# mcp-handlers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mcp-handlers` to build the library.

## Running unit tests

Run `nx test mcp-handlers` to execute the unit tests via [Jest](https://jestjs.io).
