import { examplesJsBuildable } from './examples-js-buildable';

describe('examplesJsBuildable', () => {
  it('should work', () => {
    expect(examplesJsBuildable()).toEqual('examples-js-buildable');
  });
});
