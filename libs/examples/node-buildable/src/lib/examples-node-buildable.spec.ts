import { examplesNodeBuildable } from './examples-node-buildable';

describe('examplesNodeBuildable', () => {
  it('should work', () => {
    expect(examplesNodeBuildable()).toEqual('examples-node-buildable');
  });
});
