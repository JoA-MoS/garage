# atlassian-mcp-handlers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build atlassian-mcp-handlers` to build the library.

## Running unit tests

Run `nx test atlassian-mcp-handlers` to execute the unit tests via [Jest](https://jestjs.io).
